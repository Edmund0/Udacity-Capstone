{
    "version": 2,
    "builds": [
        {
          "src": "server/server.js",
          "use": "@now/node"
        }
      ],
    "routes": [
      { "src": "/sendData", "dest": "src/server/sendData.js" },
      { "src": "/(.*)", "dest": "server/server.js" }
    ]
  }